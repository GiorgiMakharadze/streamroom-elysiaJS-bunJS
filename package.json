{
  "name": "video-streamin-elysiaj-bunjs",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/server.ts",
    "prod": "bun run dist/server.js",
    "build": "bun x tsc",
    "format": "prettier --write \"src/**/*.ts\""
  },
  "dependencies": {
    "elysia": "latest",
    "pino": "^9.6.0",
    "prettier": "^3.5.2"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}